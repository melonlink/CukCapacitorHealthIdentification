# Paper Verification v1.3

Extends the frozen v1.2 campaign with E4 TS-SRKE (supervised-reset
estimator). 6144 factorial rows; E1-E3 rows reproduce v1.2 with
max |delta| = 0 (invariance check built into the runner). Run:
`scripts/run_paper_verification_v13.m` (MATLAB R2023b, ~70 s).
Supervisor constants are predeclared in the engine configuration and
documented in `manuscript/V06_INTEGRATION_CHANGELOG.md`.
