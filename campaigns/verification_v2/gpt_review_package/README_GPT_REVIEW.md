# GPT Review Package — Verification v2

This folder is the dedicated review handoff requested by the project owner.

Start with `RESULT_V2_FOR_CHATGPT.md`, then use `V1_V2_COMPARISON.md` and
`BASELINE_REPRODUCTION.md` for the scientific argument. `result_metrics_v2.csv`
is the unified audit table. The `tables/` and `figures/` folders contain all
required CSV outputs and the 20 numbered PNG figures.

The package is generated by `../scripts/package_v2_for_gpt.m`; source code,
Simulink Model B, FIG files and reproducible MAT workspaces remain one level up.
